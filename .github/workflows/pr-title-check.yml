name: PR title check

on:
  push:
    branches: [ master ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: Slashgear/action-check-pr-title@v4.3.0
        with:
          regexp: "^\\[((ABC)-[1-9][0-9]*|No Ticket)\\] - .*$"
